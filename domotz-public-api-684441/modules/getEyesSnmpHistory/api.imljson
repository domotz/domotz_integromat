{
    "url": "/agent/{{parameters.agent_id}}/device/{{parameters.device_id}}/eye/snmp/{{parameters.sensor_id}}/history",
    "method": "GET",
    "headers": {},
    "qs": {
        "from": "{{parameters.from}}",
        "to": "{{parameters.to}}"
    },
    "response": {
        "output": "{{item}}",
        "iterate": "{{body}}"
    },
    "body": {}
}