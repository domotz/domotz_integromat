{
    "url": "/agent/{{parameters.agent_id}}/device",
    "method": "GET",
    "response": {
        "limit": 300,
        "iterate": "{{body}}",
        "output": {
            "label": "{{item.display_name}}",
            "value": "{{item.id}}"
        }
    }
}