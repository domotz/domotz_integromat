{
    "url": "/agent/{{parameters.agent_id}}/device/{{parameters.device_id}}/eye/tcp",
    "method": "GET",
    "response": {
        "limit": 300,
        "iterate": "{{body}}",
        "output": {
            "label": "{{item.port}}",
            "value": "{{item.id}}"
        }
    }
}