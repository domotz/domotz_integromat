{
    "url": "/user/{{parameters.user_id}}/alert-profile",
    "method": "GET",
    "response": {
        "limit": 300,
        "iterate": "{{body}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.id}}"
        }
    }
}