{
    "url": "/agent/{{parameters.agent_id}}/device/{{parameters.device_id}}/eye/snmp",
    "method": "GET",
    "response": {
        "limit": 300,
        "iterate": "{{body}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.id}}"
        }
    }
}